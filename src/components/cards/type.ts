export type CardProps = {
    image_path: string;
    heading: string;
    subheading: string
    link:string
} 


export type ResumeType = {
    title: string;
    id: string;
    templateId: string | null;
}